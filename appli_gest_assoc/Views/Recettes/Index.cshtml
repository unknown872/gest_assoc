@model IEnumerable<appli_gest_assoc.Models.Recette>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Libelle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Montant)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Bureau)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TypeRecette)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Libelle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Montant)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Bureau.NomBureau)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TypeRecette.Libelle)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdRecette">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdRecette">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdRecette">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
